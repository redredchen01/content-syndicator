# LAND & DEPLOY REPORT — PR #18

```
PR:           #18 — feat: Tier 1-3 OAuth syndication — WordPress.com + GitHub OAuth + Dev.to/Hashnode PAT links
Branch:       feat/tier1-3-oauth-syndication → main
Merged:       2026-05-07T03:46:53Z (squash)
Merge SHA:    312644e7f3851408dcb97c572f4c2b31ecf28bbc
Merge path:   direct (no merge queue, no auto-merge)
First run:    yes (no prior land-deploy on this slug)

Reviews:
  Eng review:  CURRENT (ce:review autofix on the same diff, 5 personas, 758/758 tests)
  Inline fix:  yes (2× P1 + 4× P2/P3, all autofixed in fix(oauth) commit)

CI:           SKIPPED (repo has no GitHub Actions workflows)
Deploy:       NO WORKFLOW (self-hosted Express app, manual restart)
Staging:      N/A
Verification: SKIPPED (no production URL configured)
  Scope:      MIXED (backend OAuth strategies + admin.html frontend + docs)

Post-merge sanity:
  Tests:      758/758 pass on merged main
  TypeScript: tsc --noEmit clean
  #17 fixes:  preserved (verified _refreshInFlight, escHtml, encryptApiKey(access_token))

VERDICT: DEPLOYED (UNVERIFIED) — code is on main, no auto-deploy infrastructure to verify against
```

## Manual verification needed

This project doesn't have CI/CD, so the user must:

1. Pull main on the machine running the server
2. Restart the Express server
3. Open `/admin.html` and verify:
   - Dev.to / Hashnode cards show "获取 API Key ↗" links
   - WordPress card shows "Connect with WordPress.com" (when env configured)
   - GitHub card shows "Connect with GitHub" (when env configured)
   - Existing Blogger / Twitter cards still work

## Post-Deploy Monitoring (from PR description)

Watch these signals over the next 24h:

- `[OAuth] insufficient_scope` log frequency (<1/day healthy)
- `[WordPress] OAuth grant revoked` / `[GitHub] OAuth token revoked` (<5/day healthy)
- `oauth_tokens` row count for new providers (grows after first user, stable thereafter)
- `/api/platforms` response shape (each platform has `patGenerationUrl: string | null`)

## Rollback trigger

Revert if (a) Blogger or Twitter OAuth (untouched here) regresses, or (b) WordPress / GitHub publish loops on 401 within 24h despite a fresh OAuth grant.
